# Vercel CLI local project link (not needed in git — Vercel manages via dashboard when connected to GitHub)
.vercel

# macOS
.DS_Store
._*
.AppleDouble
.LSOverride
Icon?

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/

# Node (in case you ever add build tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# OS + backup files
Thumbs.db
*.bak
*.tmp

# Local scratch / temp
/tmp-scratch/
